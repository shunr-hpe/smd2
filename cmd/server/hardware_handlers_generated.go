// Code generated by Fabrica v0.4.0-7-gc8d4f50. DO NOT EDIT.
// Template: server/handlers.go.tmpl
// Generated: 2026-04-22T11:55:49-06:00
//
// # Copyright © 2026 OpenCHAMI a Series of LF Projects, LLC
//
// SPDX-License-Identifier: MIT
//
// This file contains REST API handlers for Hardware resources.
//
// To modify this code:
//  1. Edit the template file: pkg/codegen/templates/handlers.go.tmpl
//  2. Run 'make dev' to regenerate
//  3. Do NOT edit this file directly - changes will be lost
//
// Generated handlers provide:
//   - GET /hardwares (list all hardwares)
//   - GET /hardwares/{uid} (get specific Hardware)
//   - POST /hardwares (create new Hardware)
//   - PUT /hardwares/{uid} (update Hardware spec)
//   - PATCH /hardwares/{uid} (patch Hardware spec)
//   - DELETE /hardwares/{uid} (delete Hardware)
//   - PUT /hardwares/{uid}/status (update Hardware status)
//   - PATCH /hardwares/{uid}/status (patch Hardware status)
//
// Authorization: Add custom middleware for authentication/authorization
// Storage: Uses storage.LoadHardware*/SaveHardware*/DeleteHardware*
// Version Support: Available (see version context in handlers)
//
// To enable full version conversion for this resource:
//  1. Add new version: fabrica add version <group> <version>
//  2. Implement converter: apis/<group>/<version>/converter.go
//  3. Add version-aware storage: storage.LoadHardwareWithVersion()
//  4. Register versions in cmd/server/main.go
package main

import (
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"time"

	v1 "github.com/OpenCHAMI/inventory-service/apis/inventory-service.openchami.org/v1"
	"github.com/go-chi/chi/v5"
	"github.com/openchami/fabrica/pkg/events"
	"github.com/openchami/fabrica/pkg/patch"
	"github.com/openchami/fabrica/pkg/resource"
	"github.com/openchami/fabrica/pkg/validation"
	"github.com/openchami/fabrica/pkg/versioning"

	"github.com/OpenCHAMI/inventory-service/cmd/plugins"
)

// GetHardwares returns all Hardware resources
func GetHardwares(w http.ResponseWriter, r *http.Request) {
	// Authorization: Add custom middleware in routes.go or implement checks here
	// Example: if !authorized(r) { respondError(w, http.StatusUnauthorized, fmt.Errorf("unauthorized")); return }

	hardwares, err := plugins.Store.LoadAllHardwares(r.Context())
	if err != nil {
		respondError(w, http.StatusInternalServerError, fmt.Errorf("failed to load hardwares: %w", err))
		return
	}
	respondJSON(w, http.StatusOK, hardwares)
}

// GetHardware returns a specific Hardware resource by UID
func GetHardware(w http.ResponseWriter, r *http.Request) {
	uid := chi.URLParam(r, "uid")
	if uid == "" {
		respondError(w, http.StatusBadRequest, fmt.Errorf("Hardware UID is required"))
		return
	}

	// Version context available here for version-aware operations
	// versionCtx := versioning.GetVersionContext(r.Context())
	// Requested version: versionCtx.ServeVersion
	// To enable: replace storage.LoadHardware() with version-aware function

	// Authorization: Add custom middleware in routes.go or implement checks here
	// Example: if !authorized(r) { respondError(w, http.StatusUnauthorized, fmt.Errorf("unauthorized")); return }

	hardware, err := plugins.Store.LoadHardware(r.Context(), uid)
	if err != nil {
		respondError(w, http.StatusNotFound, fmt.Errorf("Hardware not found: %w", err))
		return
	}
	respondJSON(w, http.StatusOK, hardware)
}

// CreateHardware creates a new Hardware resource
func CreateHardware(w http.ResponseWriter, r *http.Request) {
	var req CreateHardwareRequest
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		respondError(w, http.StatusBadRequest, fmt.Errorf("invalid request body: %w", err))
		return
	}

	// Layer 1: Request validation (validates inline spec fields and metadata)
	if err := validation.ValidateResource(&req); err != nil {
		respondError(w, http.StatusBadRequest, fmt.Errorf("validation failed: %w", err))
		return
	}

	// Get version context from request (set by version negotiation middleware)
	versionCtx := versioning.GetVersionContext(r.Context())

	uid, err := resource.GenerateUIDForResource("Hardware")
	if err != nil {
		respondError(w, http.StatusInternalServerError, fmt.Errorf("failed to generate UID: %w", err))
		return
	}

	// Versioned mode: flat fields with fabrica.Metadata
	hardware := &v1.Hardware{
		// Use negotiated ServeVersion (from Accept header) for apiVersion
		APIVersion: versionCtx.ServeVersion,
		Kind:       "Hardware",
		Spec:       req.AsSpec(),
	}

	// Initialize metadata from request
	hardware.Metadata = req.Metadata
	hardware.Metadata.UID = uid
	now := time.Now()
	hardware.Metadata.CreatedAt = now
	hardware.Metadata.UpdatedAt = now

	// Set labels and annotations
	if hardware.Metadata.Labels == nil {
		hardware.Metadata.Labels = make(map[string]string)
	}
	for k, v := range req.Labels {
		hardware.Metadata.Labels[k] = v
	}
	if hardware.Metadata.Annotations == nil {
		hardware.Metadata.Annotations = make(map[string]string)
	}
	for k, v := range req.Annotations {
		hardware.Metadata.Annotations[k] = v
	}

	// Layer 2: Custom business logic validation
	if err := validation.ValidateWithContext(r.Context(), hardware); err != nil {
		respondError(w, http.StatusBadRequest, fmt.Errorf("validation failed: %w", err))
		return
	}

	// Set initial status
	// This assumes the generator passes an 'IsReconcilable' boolean
	// to this template, and that the resource has a .Status.Phase field.

	// Save (Layer 1: Ent validation happens automatically if using Ent storage)
	if err := plugins.Store.SaveHardware(r.Context(), hardware); err != nil {
		respondError(w, http.StatusInternalServerError, fmt.Errorf("failed to save Hardware: %w", err))
		return
	}

	// Publish resource created event

	if err := events.PublishResourceCreated(r.Context(), "Hardware", hardware.Metadata.UID, hardware.Metadata.Name, hardware); err != nil {
		// Log the error but don't fail the request - events are non-critical
		fmt.Printf("Warning: Failed to publish resource created event for Hardware %s: %v\n", hardware.Metadata.UID, err)

	}

	respondJSON(w, http.StatusCreated, hardware)
}

// UpdateHardware updates the spec of an existing Hardware resource
// NOTE: This endpoint ONLY updates the spec. Use PUT //hardwares/{uid}/status to update status.
func UpdateHardware(w http.ResponseWriter, r *http.Request) {
	uid := chi.URLParam(r, "uid")
	if uid == "" {
		respondError(w, http.StatusBadRequest, fmt.Errorf("Hardware UID is required"))
		return
	}

	hardware, err := plugins.Store.LoadHardware(r.Context(), uid)
	if err != nil {
		respondError(w, http.StatusNotFound, fmt.Errorf("Hardware not found: %w", err))
		return
	}

	var req UpdateHardwareRequest
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		respondError(w, http.StatusBadRequest, fmt.Errorf("invalid request body: %w", err))
		return
	}

	// Apply updates

	// Versioned mode: direct field access
	if req.Metadata.Name != "" {
		hardware.Metadata.Name = req.Metadata.Name
	}

	// Update spec fields ONLY - status should use /status subresource
	hardware.Spec = req.AsSpec()

	// Update labels and annotations
	if hardware.Metadata.Labels == nil {
		hardware.Metadata.Labels = make(map[string]string)
	}
	for k, v := range req.Labels {
		hardware.Metadata.Labels[k] = v
	}
	if hardware.Metadata.Annotations == nil {
		hardware.Metadata.Annotations = make(map[string]string)
	}
	for k, v := range req.Annotations {
		hardware.Metadata.Annotations[k] = v
	}

	// Update timestamp
	hardware.Metadata.UpdatedAt = time.Now()

	if err := plugins.Store.SaveHardware(r.Context(), hardware); err != nil {
		respondError(w, http.StatusInternalServerError, fmt.Errorf("failed to save Hardware: %w", err))
		return
	}

	// Publish resource updated event
	updateMetadata := map[string]interface{}{
		"updatedAt": hardware.Metadata.UpdatedAt,
	}

	if err := events.PublishResourceUpdated(r.Context(), "Hardware", hardware.Metadata.UID, hardware.Metadata.Name, hardware, updateMetadata); err != nil {
		// Log the error but don't fail the request - events are non-critical
		fmt.Printf("Warning: Failed to publish resource updated event for Hardware %s: %v\n", hardware.Metadata.UID, err)

	}

	respondJSON(w, http.StatusOK, hardware)
}

// PatchHardware patches an existing Hardware resource spec using JSON Merge Patch, JSON Patch, or Shorthand Patch
// Only the spec portion of the resource can be patched - metadata and status are API-managed
func PatchHardware(w http.ResponseWriter, r *http.Request) {
	uid := chi.URLParam(r, "uid")
	if uid == "" {
		respondError(w, http.StatusBadRequest, fmt.Errorf("Hardware UID is required"))
		return
	}

	hardware, err := plugins.Store.LoadHardware(r.Context(), uid)
	if err != nil {
		respondError(w, http.StatusNotFound, fmt.Errorf("Hardware not found: %w", err))
		return
	}

	// Read patch document
	patchData, err := io.ReadAll(r.Body)
	if err != nil {
		respondError(w, http.StatusBadRequest, fmt.Errorf("failed to read patch data: %w", err))
		return
	}

	// Marshal current spec to JSON for patching (only allow spec modifications)
	currentSpecJSON, err := json.Marshal(hardware.Spec)
	if err != nil {
		respondError(w, http.StatusInternalServerError, fmt.Errorf("failed to marshal current spec: %w", err))
		return
	}

	// Detect patch type from Content-Type header
	contentType := r.Header.Get("Content-Type")
	patchType := patch.DetectPatchType(contentType)

	// Apply patch to spec only
	patchResult, err := patch.ApplyPatchWithOptions(currentSpecJSON, patchData, patchType, patch.PatchOptions{
		AllowAddFields:    true,
		AllowRemoveFields: true,
	})
	if err != nil {
		respondError(w, http.StatusUnprocessableEntity, fmt.Errorf("failed to apply patch to spec: %w", err))
		return
	}

	// Unmarshal the patched result back to the spec
	if err := json.Unmarshal(patchResult.Updated, &hardware.Spec); err != nil {
		respondError(w, http.StatusInternalServerError, fmt.Errorf("failed to unmarshal patched spec: %w", err))
		return
	}

	// Touch to update metadata

	hardware.Metadata.UpdatedAt = time.Now()

	// Save the patched resource
	if err := plugins.Store.SaveHardware(r.Context(), hardware); err != nil {
		respondError(w, http.StatusInternalServerError, fmt.Errorf("failed to save patched Hardware: %w", err))
		return
	}

	// Publish resource patched event
	patchMetadata := map[string]interface{}{
		"patchType": patchType,
		"updatedAt": hardware.Metadata.UpdatedAt,
	}

	if err := events.PublishResourcePatched(r.Context(), "Hardware", hardware.Metadata.UID, hardware.Metadata.Name, hardware, patchMetadata); err != nil {
		// Log the error but don't fail the request - events are non-critical
		fmt.Printf("Warning: Failed to publish resource patched event for Hardware %s: %v\n", hardware.Metadata.UID, err)

	}

	respondJSON(w, http.StatusOK, hardware)
}

// UpdateHardwareStatus updates only the status of a Hardware resource
// This endpoint is intended for controllers, reconcilers, and monitoring systems.
// It does not modify the spec or metadata (except updatedAt timestamp).
//
// Authorization: Requires 'update_status' permission (separate from 'update' permission)
// Events: Publishes resource updated event with updateType: "status"
func UpdateHardwareStatus(w http.ResponseWriter, r *http.Request) {
	uid := chi.URLParam(r, "uid")
	if uid == "" {
		respondError(w, http.StatusBadRequest, fmt.Errorf("Hardware UID is required"))
		return
	}

	// Authorization: Add custom middleware for status update authorization
	// Status updates can have different permissions than spec updates

	res, err := plugins.Store.LoadHardware(r.Context(), uid)
	if err != nil {
		respondError(w, http.StatusNotFound, fmt.Errorf("Hardware not found: %w", err))
		return
	}

	var statusUpdate v1.HardwareStatus
	if err := json.NewDecoder(r.Body).Decode(&statusUpdate); err != nil {
		respondError(w, http.StatusBadRequest, fmt.Errorf("invalid status body: %w", err))
		return
	}

	// Preserve spec - only update status
	res.Status = statusUpdate

	res.Metadata.UpdatedAt = time.Now()

	if err := plugins.Store.SaveHardware(r.Context(), res); err != nil {
		respondError(w, http.StatusInternalServerError, fmt.Errorf("failed to save Hardware status: %w", err))
		return
	}

	// Publish status update event
	statusMetadata := map[string]interface{}{
		"updatedAt":  res.Metadata.UpdatedAt,
		"updateType": "status",
	}

	if err := events.PublishResourceUpdated(r.Context(), "Hardware", res.Metadata.UID, res.Metadata.Name, res, statusMetadata); err != nil {
		// Log but don't fail - events are non-critical
		fmt.Printf("Warning: Failed to publish status update event for Hardware %s: %v\n", res.Metadata.UID, err)

	}

	respondJSON(w, http.StatusOK, res)
}

// PatchHardwareStatus patches only the status of a Hardware resource
// Supports JSON Merge Patch, JSON Patch, and Shorthand Patch formats.
// Only modifies status fields - spec and metadata are preserved.
func PatchHardwareStatus(w http.ResponseWriter, r *http.Request) {
	uid := chi.URLParam(r, "uid")
	if uid == "" {
		respondError(w, http.StatusBadRequest, fmt.Errorf("Hardware UID is required"))
		return
	}

	// Authorization: Add custom middleware for status patch authorization
	// Status patches can have different permissions than spec patches

	res, err := plugins.Store.LoadHardware(r.Context(), uid)
	if err != nil {
		respondError(w, http.StatusNotFound, fmt.Errorf("Hardware not found: %w", err))
		return
	}

	patchData, err := io.ReadAll(r.Body)
	if err != nil {
		respondError(w, http.StatusBadRequest, fmt.Errorf("failed to read patch data: %w", err))
		return
	}

	// Marshal current status for patching
	currentStatusJSON, err := json.Marshal(res.Status)
	if err != nil {
		respondError(w, http.StatusInternalServerError, fmt.Errorf("failed to marshal current status: %w", err))
		return
	}

	contentType := r.Header.Get("Content-Type")
	patchType := patch.DetectPatchType(contentType)

	patchResult, err := patch.ApplyPatchWithOptions(currentStatusJSON, patchData, patchType, patch.PatchOptions{
		AllowAddFields:    true,
		AllowRemoveFields: false, // Don't allow removing status fields
	})
	if err != nil {
		respondError(w, http.StatusUnprocessableEntity, fmt.Errorf("failed to apply patch to status: %w", err))
		return
	}

	// Unmarshal patched status back
	if err := json.Unmarshal(patchResult.Updated, &res.Status); err != nil {
		respondError(w, http.StatusInternalServerError, fmt.Errorf("failed to unmarshal patched status: %w", err))
		return
	}

	res.Metadata.UpdatedAt = time.Now()

	if err := plugins.Store.SaveHardware(r.Context(), res); err != nil {
		respondError(w, http.StatusInternalServerError, fmt.Errorf("failed to save patched Hardware status: %w", err))
		return
	}

	// Publish status patch event
	patchMetadata := map[string]interface{}{
		"patchType":  patchType,
		"updatedAt":  res.Metadata.UpdatedAt,
		"updateType": "status",
	}

	if err := events.PublishResourcePatched(r.Context(), "Hardware", res.Metadata.UID, res.Metadata.Name, res, patchMetadata); err != nil {
		fmt.Printf("Warning: Failed to publish status patch event for Hardware %s: %v\n", res.Metadata.UID, err)

	}

	respondJSON(w, http.StatusOK, res)
}

// DeleteHardware deletes a Hardware resource
func DeleteHardware(w http.ResponseWriter, r *http.Request) {
	uid := chi.URLParam(r, "uid")
	if uid == "" {
		respondError(w, http.StatusBadRequest, fmt.Errorf("Hardware UID is required"))
		return
	}

	// Load resource before deletion for event publishing
	hardware, err := plugins.Store.LoadHardware(r.Context(), uid)
	if err != nil {
		respondError(w, http.StatusNotFound, fmt.Errorf("Hardware not found: %w", err))
		return
	}

	if err := plugins.Store.DeleteHardware(r.Context(), uid); err != nil {
		respondError(w, http.StatusInternalServerError, fmt.Errorf("failed to delete Hardware: %w", err))
		return
	}

	// Publish resource deleted event
	deleteMetadata := map[string]interface{}{
		"deletedAt": time.Now(),
	}

	if err := events.PublishResourceDeleted(r.Context(), "Hardware", hardware.Metadata.UID, hardware.Metadata.Name, deleteMetadata); err != nil {
		// Log the error but don't fail the request - events are non-critical
		fmt.Printf("Warning: Failed to publish resource deleted event for Hardware %s: %v\n", hardware.Metadata.UID, err)

	}

	respondJSON(w, http.StatusOK, &DeleteResponse{
		Message: "Hardware deleted successfully",
		UID:     uid,
	})
}
